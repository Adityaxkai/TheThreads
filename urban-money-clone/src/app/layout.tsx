import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Threads - Premium Streetwear & Fashion",
  description: "Discover the latest trends in streetwear fashion. Shop Threads for premium clothing, accessories, and lifestyle products.",
  keywords: "streetwear, fashion, clothing, threads, premium, lifestyle",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
